// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { getConnection, getConnectionWithSecrets, getWorkspace, listConnections, } from "./connections.js";
export * from "./inputOutput.js";
function getConnections(context) {
    return {
        getWorkspace: (options) => getWorkspace(context, options),
        listConnections: (options) => listConnections(context, options),
        getConnection: (connectionName, options) => getConnection(context, connectionName, options),
        getConnectionWithSecrets: (connectionName, options) => getConnectionWithSecrets(context, connectionName, options),
    };
}
/**
 * Get the connections operations
 * @returns The connections operations
 **/
export function getConnectionsOperations(context) {
    return Object.assign({}, getConnections(context));
}
//# sourceMappingURL=index.js.map