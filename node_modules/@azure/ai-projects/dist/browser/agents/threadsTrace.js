// Copyright (c) Microsoft Corporation.
// Licensed under the MIT License.
import { TracingUtility, TracingOperationName } from "../tracing.js";
import { addMessageEvent, UpdateWithAgentAttributes } from "./traceUtility.js";
export function traceStartCreateThread(span, options) {
    TracingUtility.setSpanAttributes(span, TracingOperationName.CREATE_THREAD, UpdateWithAgentAttributes({}));
    setSpanEvents(span, options);
}
export async function traceEndCreateThread(span, _options, result) {
    const resolvedResult = await result;
    TracingUtility.updateSpanAttributes(span, { threadId: resolvedResult.id });
}
function setSpanEvents(span, options) {
    var _a;
    (_a = options.body.messages) === null || _a === void 0 ? void 0 : _a.forEach((message) => {
        addMessageEvent(span, message);
    });
}
//# sourceMappingURL=threadsTrace.js.map